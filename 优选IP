# CF_Speedtest_Tool — Cloudflare IP 测速工具

**作者：[wulaxy](https://github.com/wulaxy)**

通过 VLESS WS TLS 节点，批量测试 Cloudflare IP 的下载速度，按快慢排序输出结果。
（白话就是优选CF的IP）
---

## 工作原理

```
input/config.txt (节点配置)  ──┐
input/ip.txt     (IP 列表)  ──┤──→ 逐 IP 生成 Xray 配置 → 启动 Xray 本地代理
                               │                           ↓
                               └──→ 通过代理下载测试文件 → 记录速度 → 写入 results/
```

1. 从 `input/config.txt` 读取 VLESS 节点信息 + 测速参数
2. 从 `input/ip.txt` 读取待测 IP 列表
3. 对每个 IP，生成对应的 Xray 出站配置
4. 在本地启动 HTTP 代理（默认 `127.0.0.1:10808`）
5. 通过代理下载测试文件，记录速度
6. 结果写入 `results/speed_results.txt`（从快到慢排序）

---

## 项目结构

```
├── input/
│   ├── config.txt          # 节点配置（VLESS 链接）+ 测速参数
│   └── ip.txt              # 待测 IP 列表（每行一个）
├── src/
│   ├── speedtest.py        # 主程序
│   └── cores/              # Xray 核心（各平台）
│       ├── Xray-linux-64/
│       ├── Xray-linux-arm64-v8a/
│       └── Xray-windows-64/
├── launchers/              # 平台启动器
│   ├── linux/
│   │   ├── start-linux-64.sh
│   │   └── start-linux-arm64.sh
│   └── windows/
│       └── start-speedtest-windows.bat
├── results/                # 测速结果（每次运行生成）
│   └── speed_results.txt
├── logs/                   # 运行日志（每次运行生成）
│   └── speedtest_run.log
├── output/
│   └── runtime/            # Xray 临时配置（运行时生成）
├── README.md
└── .gitignore
```
不想要那么多核心，可以删了
---

## 快速开始

### Windows

双击或在命令行运行：

```bat
launchers\windows\start-speedtest-windows.bat
```

### Linux x64

```sh
sh launchers/linux/start-linux-64.sh
```

### Linux arm64

```sh
sh launchers/linux/start-linux-arm64.sh
```

启动器会自动找到 Python 和对应平台的 Xray 核心，然后运行 `src/speedtest.py`。

---

## 配置说明

所有配置集中在 `input/` 目录下的两个文件中。

### 1. 节点 + 测速参数 (`input/config.txt`)

```
# 节点配置（VLESS 链接）
# 第一行有效内容必须是 VLESS 链接
# 将你的 VLESS 链接粘贴到下面，替换 ** 占位符
vless://**@**:****?encryption=none&security=tls&sni=**&fp=ios&type=ws&host=**&path=**

# 测速地址（可选，默认 http://cachefly.cachefly.net/50mb.test）
test_url=http://cachefly.cachefly.net/50mb.test

# 测速参数（可选，不设置则使用默认值）
default_target_port=443
local_http_port=10808
max_test_seconds=8
max_download_mb=20
xray_start_timeout=6
request_timeout=15
chunk_kb=256
```

#### VLESS 链接格式详解

VLESS 链接的通用格式：

```
vless://**@**:****?参数
```

URL 参数说明（会被脚本自动解析）：

| 参数 | 对应含义 | 说明 |
|---|---|---|
| `encryption` | 加密方式 | 通常为 `none` |
| `security` | 传输层安全 | `tls` |
| `sni` | TLS SNI（域名） | 你的节点域名 |
| `fp` | TLS 指纹 | `ios` / `chrome` / `firefox` |
| `type` | 传输协议 | `ws`（WebSocket） |
| `host` | WebSocket Host 头 | 你的节点域名 |
| `path` | WebSocket 路径 | 你的节点路径（需 URL 编码） |

> 程序读取逻辑：文件中第一个以 `vless://` 开头的非注释行视为 VLESS 链接；后续的 `key=value` 行视为测速参数。

#### 测速参数说明

| 参数 | 说明 | 默认值 |
|---|---|---|
| `test_url` | 下载测速文件地址 | `http://cachefly.cachefly.net/50mb.test` |
| `default_target_port` | IP 未指定端口时的默认值 | `443` |
| `local_http_port` | Xray 本地代理端口 | `10808` |
| `xray_start_timeout` | 等待 Xray 启动的超时（秒） | `6` |
| `request_timeout` | HTTP 请求超时（秒） | `15` |
| `max_test_seconds` | 每个 IP 最大测速时间（秒） | `8` |
| `max_download_mb` | 每个 IP 最大下载量（MB） | `20` |
| `chunk_kb` | 下载读取块大小（KB） | `256` |

### 2. IP 列表 (`input/ip.txt`)

每行一个目标，支持以下格式：

**IP:端口**
```
104.16.2.2:443
1.1.1.1:443
```

**仅 IP（使用 `default_target_port`）**
```
104.17.1.1
```

**IPv6**
```
[2606:4700::6810:0202]:443
```

**注释（以 `#` 开头）**
```
# AWS CloudFront IP
18.183.224.152:443
```

---

## 输出说明

### 测速结果 (`results/speed_results.txt`)

所有 IP 测速完成后，按速度从快到慢排序：

```
=== CF VLESS WS TLS 测速结果（从快到慢） ===
测试地址: http://cachefly.cachefly.net/50mb.test
生成时间: 2025-01-15 14:35:02

IP:Port	速度(MB/s)	耗时(s)	下载量	状态
104.16.2.2:443	5.12	3.91	20.00 MB	OK
1.1.1.1:443	3.87	5.17	20.00 MB	OK
104.17.1.1:443	0.00	8.02	0.00 B	失败: timeout
```

| 列 | 说明 |
|---|---|
| `IP:Port` | 测试的 IP 地址和端口 |
| `速度(MB/s)` | 平均下载速度 |
| `耗时(s)` | 测试用时 |
| `下载量` | 实际下载数据量 |
| `状态` | `OK` 或 `失败: 原因` |

### 运行日志 (`logs/speedtest_run.log`)

实时记录每个 IP 的启动、下载进度和结果：

```
[2025-01-15 14:30:01] CF_Speedtest_Tool 启动
[2025-01-15 14:30:01] 配置文件: input/config.txt
[2025-01-15 14:30:01] 节点: 1.1.1.1:443
[2025-01-15 14:30:01] IP 列表: input/ip.txt (3 个目标)
[2025-01-15 14:30:01] Xray 核心: src/cores/Xray-linux-64/xray
[2025-01-15 14:30:01] 测速地址: http://cachefly.cachefly.net/50mb.test
[2025-01-15 14:30:01] 运行日志: logs/speedtest_run.log
[2025-01-15 14:30:01] 结果文件: results/speed_results.txt
[2025-01-15 14:30:01]
[2025-01-15 14:30:01] [1/3] 启动 Xray，目标: 104.16.2.2:443
[2025-01-15 14:30:04] [1/3] Xray 就绪。测试下载: http://cachefly.cachefly.net/50mb.test
[2025-01-15 14:30:04] [1/3] 104.16.2.2:443 下载中: 0.99 MB / 50.00 MB, 4.21 MB/s
[2025-01-15 14:30:09] 5.12 MB/s (20.00 MB, 3.91s)
[2025-01-15 14:30:09] 部分结果已保存: results/speed_results.txt
```

---

## 自定义节点

只需修改 `input/config.txt` 中的 VLESS 链接即可更换节点。从客户端复制 VLESS 链接，替换文件中的 `**` 占位符即可。

> 大多数客户端（v2rayN、Shadowrocket、Stash 等）都支持复制 VLESS 链接，直接粘贴替换 `**`。

---

## 注意事项

- **Xray 核心**：位于 `src/cores/`，来自 [XTLS/Xray-core](https://github.com/XTLS/Xray-core)，遵循其开源许可
- **系统要求**：Python 3.8+，支持 Linux / Windows
- **端口冲突**：默认 `10808` 端口，可在 `config.txt` 中修改
- **运行方式**：启动器会自动设置环境变量，也可直接运行 `python3 -u src/speedtest.py`
